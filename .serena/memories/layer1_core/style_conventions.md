# コードスタイル/規約
- 最小実装を優先し、不要な機能・依存・ファイルを増やさない。
- 重複ロジックは `src/core.ts` に集約（DRY）。
- ハードコード禁止。設定値は `config/*.yaml` を `src/core.ts` 経由で取得。
- Fail Fast: 例外を握りつぶさず、早期に失敗させる。
- すべてのエージェントクラスは `BaseAgent`（src/core.ts）を継承。
- ドキュメントは日本語で記述。
- TypeScript で `any` を使わない（厳格な型定義）。
- コメントは最小限。コード自体を自己説明的に保つ。