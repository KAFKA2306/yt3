---
description: Remove unused code, comments, error handling. Use for "clean" or "simplify" requests.
---
# Cleanup Workflow

1.  **Read Rules**: Open and read `.agent/skills/cleanup/SKILL.md` to understand the "Iron Rules".
2.  **Execute**: Follow the strict `Delete` and `Keep` rules defined in that skill.
3.  **Verify**: Run `npm run lint`.
